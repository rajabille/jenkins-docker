You might not using the similar kind of setup, so you have to make sure GID (Group ID) for docker accordingly in your machine
To check run...
cat /etc/group | grep docker

to find the GID of docker in your system